"""Shared config helpers (yaml load/merge). Experiment configs live under experiments/*/config.yaml."""

# TODO: load_config(path, overrides) -> load yaml, apply CLI/dict overrides, return a plain dict/dataclass.
