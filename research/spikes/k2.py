"""K2 spike set: the curated benchmark of spikes (natural + induced) the paper reports on.

Each entry: trunk id, step, snapshot ref, cause label. The fixed evaluation set for C1/C2.
"""

# TODO: build_k2(trunks) -> collect + label spikes into the canonical K2 manifest.
# TODO: load_k2() -> iterate (snapshot, label) pairs for localizer/repair evaluation.
