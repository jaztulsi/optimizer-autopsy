"""Statistics: significance tests + confidence intervals over the fork sweep.

The N x methods x seeds design needs paired tests and multiple-comparison correction.
"""

# TODO: paired_test(method_a, method_b) -> paired stat + p-value across matched forks/seeds.
# TODO: bootstrap_ci(values) -> confidence interval for a reported metric.
# TODO: correct(pvalues) -> multiple-comparison correction across the method matrix.
