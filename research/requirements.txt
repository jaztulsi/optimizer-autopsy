# Pinned to Kaggle's default GPU image. Single source of truth: research.harness.preflight.check_env()
# parses this file and asserts installed == pinned. To reconcile with a new Kaggle image, run
# check_env() there and paste the versions it reports as mismatched.
torch==2.4.0
numpy==1.26.4
scipy==1.14.0
safetensors==0.4.3
huggingface_hub==0.24.0
datasets==2.20.0
tiktoken==0.7.0
wandb==0.17.5
pyyaml==6.0.1
tqdm==4.66.4
