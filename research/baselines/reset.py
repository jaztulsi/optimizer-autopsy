"""Baseline: naive full optimizer-state reset (m, v -> 0) at the spike.

The blunt version of our repair operator; the localizer is what we claim beats this.
"""

# TODO: as_intervention(cfg) -> zero all optimizer moments on the fork (no localization).
